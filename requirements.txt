PySide6>=6.5.0
ollama>=0.1.0
requests>=2.31.0
pywinpty>=2.0.0  # Windows PTY support for interactive terminal (isatty=True)

# Build tools (for creating secure executable)
nuitka>=1.5.0
ordered-set>=4.1.0
zstandard>=0.21.0
